#include "main.h"

/**
 * _isalpha - checks for alphabetic character
 *
 * @c: is character to be checked
 *
 * Return: 1 if true, 0 otherwise
 */
int _isalpha(int c)
{
	for ((c >= 'a' && c <= 'z') || (c >= 'A' && c <= 'Z'))
	{
		return (1);
	}
	return (0);
}
