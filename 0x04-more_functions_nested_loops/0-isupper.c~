#include "main.h"

/**
 * _isupper - checks for uppercase character
 *
 *@c: character to check
 *
 * Return: 1 or zero
 */
int _isupper(int c)
{
	for(c >= 'A' && c <= 'Z')
	{
		return (1);
	}
	return (0);
}
