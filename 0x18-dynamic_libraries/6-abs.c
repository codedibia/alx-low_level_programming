#include "main.h"
/**
 * _abs - main function
 *
 * @n: integer to be made absolute
 *
 * Return: int or int multiplied by negative
 */

int _abs(int n)
{
	if (n > 0)
	{
		return (n);
	}
	return (n * -1);
}
